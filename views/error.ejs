<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Error</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #1a1a1a;
      color: #fff;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }
    .error-box {
      background: #2a2a2a;
      border: 2px solid #e74c3c;
      border-radius: 10px;
      padding: 30px;
      max-width: 500px;
      text-align: center;
      box-shadow: 0 0 15px rgba(231, 76, 60, 0.6);
    }
    h1 {
      margin: 0 0 10px;
      font-size: 2em;
      color: #e74c3c;
    }
    p {
      margin: 0 0 20px;
      font-size: 1.2em;
    }
    a {
      display: inline-block;
      padding: 10px 20px;
      background: #e74c3c;
      color: white;
      text-decoration: none;
      border-radius: 6px;
      font-weight: bold;
      transition: background 0.3s;
    }
    a:hover {
      background: #c0392b;
    }
  </style>
</head>
<body>
  <div class="error-box">
    <h1>⚠️ Error</h1>
    <p><%= message %></p>
    <a href="<%= redirectTo %>">Volver</a>
  </div>
</body>
</html>
